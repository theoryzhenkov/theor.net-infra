# Plaintext template — encrypt with:
#   SOPS_AGE_KEY_FILE=../.age-key.txt sops terraform/secrets.enc.yaml
#
# After encrypting, this comment block will be replaced by sops metadata.

hcloud_token: REPLACE_ME
sops_age_private_key: REPLACE_WITH_CONTENTS_OF_AGE_KEY_TXT
